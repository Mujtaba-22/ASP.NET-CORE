@using System.Globalization
@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<SharedResource> SharedLocalizer
@model List<Product>

@{
     Layout="_DashboardLayout";
}
 
<div class="container mt-4">

    <div class="card">
        <div class="card-header">
            <h5>
                <i class="bi bi-plus-circle-fill"></i>  @SharedLocalizer["AddNewProducts"]
            </h5>
        </div>

        <div class="card-body">
            <form asp-action="Create" method="post" enctype="multipart/form-data">
              @Html.AntiForgeryToken()
                <div class="mb-3">
                    <label class="form-label"> @SharedLocalizer["SelectCategories"]  </label>
                    <select class="form-select" name="CategoryId">
                      @foreach(var item in ViewBag.categories)
                      {
                        <option value="@item.Id">
                            @item.Name
                        </option>
                      }
                    </select>
                </div>
              <div class="mb-3">
                <label class="form-label"> @SharedLocalizer["ProductName"]   </label>
                <input type="text" name="Name" placeholder="@SharedLocalizer["EnterProductName"]" class="form-control"  />
                <span asp-validation-for="@Model.First().Name" class="text-danger"></span>
              </div>

               <div class="mb-3">
                <label class="form-label"> @SharedLocalizer["Price"] </label>
                <input type="number" name="Price" step="0.01" value="0" placeholder="@SharedLocalizer["EnterPrice"]" class="form-control"  />
                <span asp-validation-for="@Model.First().Price" class="text-danger"></span>
              </div>

              <div class="mb-4">
                    <label class="form-label fw-semibold"> @SharedLocalizer["Image"] </label>
                    <input name="ImageFile" type="file" class="form-control" accept="image/*" />
                    @if (Model != null && Model.Any())
                    {   
                        <span asp-validation-for="@Model.First().ImageFile" class="text-danger"></span>
                    }
                </div>


              <div class="mb-3">
               
               <button class="btn btn-success px-4 fw-bload">@SharedLocalizer["AddNewProducts"]</button>
               <a asp-action="Index" class="btn btn-light">@SharedLocalizer["Cancel"]</a>
                
              </div>
            </form>
        </div>
    </div>

   

</div>
 <div class="card mt-5">
        <div class="card-body">
          <div class="card mb-3 border-0 bg-light p-3 shadow-sm">
            <form method="get" asp-action="Index" class="row g-3 align-items-end">
                <div class="col-md-9">
                    <label class="form-label small fw-bold text-secondary"> @SharedLocalizer["SearchProductName"] </label>
                    <input type="text" name="searchTerm" value="@ViewBag.CurrentSearch" placeholder="@SharedLocalizer["EnterProductName"]" class="form-control" />
                </div>
                <div class="col-md-3 d-flex gap-2">
                    <button type="submit" class="btn btn-primary w-100 fw-bold">
                        <i class="bi bi-search"></i> @SharedLocalizer["Search"]
                    </button>
                    <a asp-action="Index"  class="btn btn-secondary">@SharedLocalizer["Cancel"]</a>
                </div>
            </form>
</div>
            <table class="table">
             <thead>
                  <tr>
                    <td>#</td>
                    <td>@SharedLocalizer["ProductName"]</td>
                    <td>@SharedLocalizer["Price"]</td>
                     <td>@SharedLocalizer["Image"]</td>
                    <td>@SharedLocalizer["Category"]</td>
                    <td colspan="2">@SharedLocalizer["Actions"]</td>
                  </tr>
             </thead>

             <tbody>
              @if(Model==null )
              {
                <tr>
                    <td>@SharedLocalizer["NoProductsAvailable"]</td>
                </tr>
              }

              else{
                @foreach(var item in Model)
                {
                    <tr>
                        <td class="ps-4 fw-bload text-secandory">@item.Id</td>
                        <td class="ps-4 fw-bload text-secandory">@item.Name</td>
                        <td class="ps-4 fw-bload text-secandory">@item.Price</td>
                         <td class="ps-4 fw-bload text-secandory">
                          <img src="~/Images/@item.Images" width="50" height="50" class="rounded" style="object-fit: cover;" />
                         </td>
                         <td class="ps-4 fw-bload text-secandory">@item.Categories.Name</td>
                        <td> <a asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square text-success"></i></a></td>
                        <td> <a  asp-action="Delete"  asp-route-id="@item.Id" onclick="return confirm('@SharedLocalizer["ConfirmDelete"]')"><i class="bi bi-trash3 text-danger"></i></a></td>
                    </tr>
                }
              }
             </tbody>
            </table>
        </div>
    </div>